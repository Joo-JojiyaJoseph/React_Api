import  React from "react";
import Slider from "../components/home/Slider";

const Home = () => {
  // const { name } = useContext(AppContext);
  
  return (
    <div>
  <Slider/>
    </div>
  );
};

export default Home;
